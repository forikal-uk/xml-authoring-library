{
    "name": "forikal-uk/xml-authoring-library",
    "description": "Holding code that is reused by more than one component of the forikal xml authoring suite of projects.",
    "type": "library",
    "license": "Apache-2.0",
    "require": {
        "php": ">=5.5.9",
        "psr/log": "^1.0",
        "symfony/filesystem": "^3.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^4.8",
        "google/apiclient": "^2.0",
        "symfony/console": "^3.4"
    },
    "suggest": {
        "google/apiclient": "To use the Google API helper (version ^2.0 is required)",
        "symfony/console": "To use the Symfony Console helpers (version ^3.4 is required)"
    },
    "autoload": {
        "psr-4": {
            "Forikal\\Library\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Forikal\\Library\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit"
    }
}
